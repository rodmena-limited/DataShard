# #87 — [0.10.0] Absolute URIs in metadata: location as URI, boundary conversion, moved-table tolerance, relocate command

EARS SPEC:
- New tables shall record location as a URI (file:///abs/path locally, s3://bucket/prefix on S3) and every manifest-list, manifest_path and file_path as location + '/' + table-relative path.

Design input: SPECS/83-iceberg-v2-spike.md; plan /home/farshid/.claude/plans/ok-let-s-plan-for-greedy-rainbow.md (0.10.0).

## Outcome
(filled at release)
